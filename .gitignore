# Cache and temporary files
cache/
debug/
file-history/
paste-cache/
shell-snapshots/
statsig/
todos/
image-cache/

# Session logs and history (local state, not synced)
sessions/
history.jsonl
stats-cache.json
projects/
contexts/
pause-state.json
session-env/

# 1Code worktree state (local)
.1code/

# MCP config (machine-specific)
mcp-config.json

# Plugins (installed locally, not synced)
plugins/installed_plugins.json
plugins/known_marketplaces.json
plugins/marketplaces/

# Sync daemon node_modules
sync/daemon/node_modules/

# Settings (may contain sensitive data)
settings.json
settings.local.json

# Environment files
.env
.env.*
.env.local
!.env.example
.env*.local

# Plugin marketplace structure (required for installation)
# plugins/claude-codex/ contains symlinks to root content

# Plan files (temporary)
plans/
tasks/
.vercel

# OS files
.DS_Store

# Credentials (sensitive)
.credentials.json

# Security warning state files (per-session, not synced)
security_warnings_state_*.json

# Chrome extension local state
chrome/

# Plugin install caches
plugins/install-counts-cache.json

# Changelog config (machine-specific)
changelog-config.json
